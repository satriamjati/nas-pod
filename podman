account management dulu di Dockerfile atau bisa nanti setelah db copy

podman build -t nas-image
podman run -d --name nas --replace --network host -v /srv/nas/share:/mnt/1 -v /mnt/data/nas/share:/mnt/2  -v  ~/svc/nas/smb.conf:/etc/samba/smb.conf:ro  localhost/nas-image:latest


make persistent db after run without db first (should run it first above):

sudo mkdir -p /srv/nas/config/private
sudo podman cp nas:/var/lib/samba/private/. /srv/nas/config/private/

sudo podman run -d --name nas --replace --network host \
  -v /srv/nas/share:/mnt/1 \
  -v /mnt/data/nas/share:/mnt/2 \
  -v ~/svc/nas/smb.conf:/etc/samba/smb.conf:ro \
  -v /srv/nas/config/private:/var/lib/samba/private:Z \
  localhost/nas-image:latest

